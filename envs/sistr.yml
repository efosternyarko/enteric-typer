name: enteric_sistr
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - sistr_cmd=1.1.2
  - blast
  - setuptools<71  # pkg_resources bundled in setuptools <71; removed in newer builds
