name: enteric_sistr
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - sistr_cmd=1.1.2
  - blast>=2.10,<2.15  # BLAST 2.15+ output format breaks SISTR 1.1.2 parser
  - setuptools<71  # pkg_resources bundled in setuptools <71; removed in newer builds
