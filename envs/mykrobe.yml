name: mykrobe
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - mykrobe=0.13.0
  - setuptools<70
  - requests>=2.28,<2.32  # pin to help solver avoid urllib3/c-ares conflict chain
  - urllib3>=1.26,<2
