name: shigeifinder
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python<3.12     # pkg_resources removed from stdlib in 3.12+; shigeifinder 1.3.5 requires it
  - setuptools      # provides pkg_resources
  - shigeifinder=1.3.5
