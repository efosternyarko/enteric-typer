name: shigeifinder
channels:
  - bioconda
  - conda-forge
  - nodefaults
dependencies:
  - python<3.12     # shigeifinder 1.3.5 uses pkg_resources which needs setuptools<71
  - setuptools<71   # setuptools>=71 removed pkg_resources as a top-level module
  - shigeifinder=1.3.5
