// ── Default parameters ────────────────────────────────────────────────────────
params {
    // Input — one of these is required
    samplesheet   = null
    input_dir     = null

    // Output
    outdir        = 'results'

    // Phylogenetics
    ska2_min_samples    = 3      // skip SKA2/IQ-TREE if fewer samples per species
    ska2_prop_filter    = 0.95   // core-genome filter (present in ≥ X fraction of samples)
    iqtree_model        = 'MFP'
    iqtree_bootstraps   = 1000
    skip_local_phylo    = false  // set true to skip SKA2 + IQ-TREE
}

// ── Process defaults ──────────────────────────────────────────────────────────
process {
    publishDir = [
        path: { "${params.outdir}/${task.process.tokenize(':')[-1].toLowerCase()}" },
        mode: 'copy',
        saveAs: { filename -> filename.startsWith('NO_FILE') ? null : filename }
    ]

    errorStrategy = { task.exitStatus in [1, 2, 14] ? 'ignore' : 'terminate' }
    maxRetries    = 1

    withLabel: 'low' {
        cpus   = 2
        memory = '4 GB'
        time   = '1h'
    }
    withLabel: 'medium' {
        cpus   = 4
        memory = '16 GB'
        time   = '4h'
    }
    withLabel: 'high' {
        cpus   = 8
        memory = '20 GB'
        time   = '8h'
    }
}

// ── Profiles ──────────────────────────────────────────────────────────────────
profiles {

    conda {
        conda.enabled  = true
        conda.useMamba = false
    }

    mamba {
        conda.enabled  = true
        conda.useMamba = true
    }

    // macOS Apple Silicon (M1/M2/M3/M4) — combine with conda or mamba:
    //   nextflow run main.nf -profile conda,arm64
    // Some bioconda packages (e.g. abricate) have no native osx-arm64 build.
    // This profile forces osx-64 conda envs, which run transparently via Rosetta 2.
    arm64 {
        conda.createOptions = '--platform osx-64'
    }

    docker {
        docker.enabled      = true
        docker.fixOwnership = true
        docker.runOptions   = '-u $(id -u):$(id -g)'
    }

    singularity {
        singularity.enabled    = true
        singularity.autoMounts = true
        singularity.cacheDir   = "${HOME}/.singularity/cache"
        singularity.pullTimeout = '2h'
    }

    slurm {
        process.executor = 'slurm'
        process.queue    = 'comp'
        withLabel: 'low'    { cpus = 2;  memory = '4 GB';  time = '1h'  }
        withLabel: 'medium' { cpus = 4;  memory = '16 GB'; time = '4h'  }
        withLabel: 'high'   { cpus = 16; memory = '64 GB'; time = '12h' }
    }

    pbs {
        process.executor = 'pbs'
        process.queue    = 'normal'
        withLabel: 'low'    { cpus = 2;  memory = '4 GB';  time = '1h'  }
        withLabel: 'medium' { cpus = 8;  memory = '16 GB'; time = '4h'  }
        withLabel: 'high'   { cpus = 16; memory = '64 GB'; time = '12h' }
    }

    test {
        params.samplesheet = "${projectDir}/test/samplesheet.csv"
        params.outdir      = 'test_results'
    }
}

// ── Reporting ─────────────────────────────────────────────────────────────────
timeline {
    enabled   = true
    file      = "${params.outdir}/pipeline_info/timeline.html"
    overwrite  = true
}
report {
    enabled   = true
    file      = "${params.outdir}/pipeline_info/report.html"
    overwrite  = true
}
dag {
    enabled   = true
    file      = "${params.outdir}/pipeline_info/dag.svg"
    overwrite  = true
}
